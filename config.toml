[solution]
name = "solution-v1"      # Solution name
definition = "fused_moe"          # Track: fused_moe | sparse_attention | gated_delta_net
author = "Shaymin"              # Team/author name

[build]
language = "cuda"               # triton | cuda
entry_point = "binding.py::kernel"            # Kernel function name
